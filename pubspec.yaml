name: conquer_dektop_daemon
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  logging: ^1.3.0
  path: ^1.9.1
  dbus: ^0.7.0
  win32: ^5.10.0
  ffi: 2.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
